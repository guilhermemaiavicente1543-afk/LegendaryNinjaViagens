export default function EntryHall({
  userEmail,
  onOpenMyNinja,
  onOpenMap,
  onOpenAdmin,
  onLogout
}) {
  return (
    <section className="ln-landing">
      <header className="ln-landing-nav">
        <div className="ln-brand-mark">
          <strong>LN</strong>
          <span>伝説</span>
        </div>

        <nav className="ln-nav-links" aria-label="Navegação principal">
          <button type="button" className="active">Início</button>
          <button type="button" onClick={onOpenMap}>Mapa</button>
          <button type="button" onClick={onOpenMyNinja}>Personagem</button>
          <button type="button">Missões</button>
          <button type="button">Arquivo</button>
          <button type="button">Rankings</button>
          <button type="button">Comunidade</button>
        </nav>

        <div className="ln-nav-actions">
          <button type="button" className="ln-login-button" onClick={onOpenMyNinja}>
            Entrar
          </button>

          <button type="button" className="ln-user-button" title={userEmail || "Usuário"}>
            忍
          </button>
        </div>
      </header>

      <div className="ln-manga-strip left">
        <div className="ln-panel eye">眼</div>
        <div className="ln-panel village">影は力なり</div>
        <div className="ln-panel kanji">忍</div>
      </div>

      <div className="ln-side-banner">
        <span>忍びの道</span>
      </div>

      <main className="ln-hero">
        <div className="ln-hero-orbit">
          <span>LN</span>
        </div>

        <p className="ln-hero-kicker">Sistema Shinobi Online</p>

        <h1>
          <span>LN</span> Digital
        </h1>

        <p className="ln-subtitle">
          Legendary Ninja Digital
        </p>

        <p className="ln-hero-description">
          Crie seu ninja, percorra o mapa, desbloqueie habilidades e acompanhe
          sua jornada em uma plataforma online de RPG shinobi.
        </p>

        <div className="ln-hero-actions">
          <button type="button" className="primary" onClick={onOpenMyNinja}>
            Entrar
          </button>

          <button type="button" className="secondary" onClick={onOpenMap}>
            Explorar Mapa
          </button>

          <button type="button" className="ghost" onClick={onOpenAdmin}>
            Painel ADM
          </button>
        </div>
      </main>

      <footer className="ln-landing-footer">
        <div className="ln-status-card">
          <strong>Status do Sistema</strong>
          <span className="online">● Online</span>

          <div>
            <p>
              <b>Players</b>
              <span>Ativos</span>
            </p>

            <p>
              <b>Mapa</b>
              <span>Em operação</span>
            </p>
          </div>
        </div>

        <button type="button" className="ln-logout" onClick={onLogout}>
          Sair
        </button>
      </footer>
    </section>
  );
}
