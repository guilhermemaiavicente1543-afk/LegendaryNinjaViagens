export default function EntryHall({
  userEmail,
  onOpenMyNinja,
  onOpenMap,
  onOpenAdmin,
  onLogout
}) {
  return (
    <section className="entry-hall-page">
      <div className="entry-hall-card">
        <p className="eyebrow">LN Digital</p>
        <h1>Legendary Ninja Digital</h1>
        <p>
          Hall de entrada da plataforma. Escolha qual área deseja acessar.
        </p>

        <div className="entry-user-box">
          <span>Logado como</span>
          <strong>{userEmail || "Modo demonstração"}</strong>
        </div>

        <div className="entry-actions entry-actions-three">
          <button type="button" onClick={onOpenMyNinja}>
            Meu Ninja
            <span>Ficha, informações, localização e teia do personagem</span>
          </button>

          <button type="button" onClick={onOpenMap}>
            Mapa de Viagem
            <span>Mapa separado para calcular rotas e iniciar viagens</span>
          </button>

          <button type="button" onClick={onOpenAdmin}>
            Painel ADM
            <span>Personagens, cupons, aparências, mapa ADM e editor da teia</span>
          </button>
        </div>

        <button type="button" className="entry-logout-button" onClick={onLogout}>
          Sair
        </button>
      </div>
    </section>
  );
}
